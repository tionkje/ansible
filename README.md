For testing:

```bash
docker build -t arch-setup . && docker run --rm -it arch-setup bash -c 'ansible-playbook local.yml && zsh'
```


to install:
- yay
- keepassxc
- insync
- slack-desktop
- spotify
- google-chrome
- google-chrome-dev
- google-chrome-beta
